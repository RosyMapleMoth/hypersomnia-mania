using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class startOffScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        UiGameManager.startGame();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
